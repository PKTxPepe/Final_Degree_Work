using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FireWoord_Script : MonoBehaviour
{
    public string color;
    private GameObject marshmallow;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    /*void OnTriggerEnter(Collider other)
    {
        if(other.gameObject.CompareTag("Marshmallow"))
        {

        }

    }*/

    /*void OnTriggerStay(Collider other)
    {
        while(itemCooking == other.gameObject)
        {
            float timer = 0;

            while(timer <= cookTime)
            {
                float lerp = Mathf.Lerp(0, 1, timer / cookTime);
                rend
            }
        }
    }*/
    
}
